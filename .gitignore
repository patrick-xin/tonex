node_modules
.next
dist
.turbo
.pnpm-store
*.tsbuildinfo
.DS_Store
coverage
.claude/settings.local.json
.claude/worktrees/

# Private docs — vision/positioning, strategic notes not for public repo
docs/private/
NOTES.md

# Per-developer IDE settings
.vscode/

# Playwright E2E artifacts (apps/www) — reports/traces/results, never committed.
# Browser binaries live in ~/.cache/ms-playwright (global), not the repo.
playwright-report/
blob-report/
test-results/
.last-run.json

# Local knowledge-graph snapshots (graphify) — not committed; kept as
# point-in-time references for "what changed" comparisons across sessions.
graphify-out/
scratch/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# env files (secrets — never commit)
.env*
!.env.example

# fallow churn-analysis cache
.fallow/
