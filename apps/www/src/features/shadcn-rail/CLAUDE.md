# shadcn-rail

The rail UI standard (typography, spacing, sizing, radius, header anatomy) is canonical in **`../md-rail/CLAUDE.md`** and **governs both rails** — md-rail is the rail template per ADR-0019. Follow it here; don't fork a separate style.
