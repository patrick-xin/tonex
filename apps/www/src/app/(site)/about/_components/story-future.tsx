export function StoryFuture() {
  return (
    <div className="space-y-4 border-t border-primary/20 grid grid-cols-1 relative">
      <div className="p-6 md:p-14 space-y-4">
        <h2 className="text-3xl font-semibold">The future</h2>
        <p>
          The design stack is growing new carriers for these decisions — DESIGN.md, SKILL.md, token
          files an agent can read and pass along. They move colors farther and faster, but a carrier
          only transports what already has shape. It can't decide whether the colors belong
          together; that still has to be authored by something.
        </p>
        <p>
          Tonex is that something. It sits just before the handoff:{' '}
          <span className="font-semibold">an authored color source</span> — roles, constraints,
          light and dark behavior, contrast that holds — that an agent can keep building on long
          after the first screen. The places fast work usually breaks — dark mode, disabled states,
          elevated surfaces, charts, the feature someone adds three weeks later — are the ones it's
          built to survive.
        </p>
        <p>
          That’s the future Tonex is built for: humans choose the feeling, agents do the labor, and
          underneath both sits a color system with a point of view — so moving fast and meaning it
          stop being a trade-off.
        </p>
      </div>
    </div>
  )
}
